#include<stdio.h>
int main(){
    // Calculating percentage of 5 subjects
    float math = 99;
    float phy = 70;
    float chem = 90;
    float hindi = 50;
    float english = 70;
    float  SumOfNumbers = (math+ phy+ chem+ hindi+ english)/5;
    printf("Percentage of 5 subjects is : %f", SumOfNumbers);
    
    // This a speacle character:- !@#$%^&*(){}[] :;""?  this is not allow in c language;

    return 0;
}

// 1) Variable can start from an alphabatet or underscrore _;
// Speacil charcters exccept _ are not allowed;
// Some particular keywords are not allowed;
// Commas or blanks are not allowed;
// Auto double int break extern enum unsigned while case sizeof for const static long continu else signed do short witch char volating goto struct if union return void registation 